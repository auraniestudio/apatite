# Contact form: POST JSON { name, email, message, token }
# Production builds only — full enquiries URL (dev uses Vite /api/enquiries proxy).
# VITE_ENQUIRY_ENDPOINT=https://5350sqjspa.execute-api.ap-southeast-2.amazonaws.com/prod/enquiries
